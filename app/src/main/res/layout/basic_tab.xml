<?xml version="1.0" encoding="utf-8"?>
<TabHost xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@android:id/tabhost"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">
    <!-- android:fitsSystemWindows="true" android:clipToPadding="true" -->

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:orientation="vertical">
        <!--
        <LinearLayout android:id="@+id/notification_padding_layout" android:background="@drawable/bg_noticefication" android:layout_height="0dp" android:layout_weight="0" android:layout_width="fill_parent" android:orientation="horizontal" />
         -->

        <RelativeLayout
            android:layout_width="fill_parent"
            android:layout_height="fill_parent">

            <TabWidget
                android:id="@android:id/tabs"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:layout_alignParentBottom="true"
                android:layout_margin="0dp"
                android:background="@color/white"
                android:divider="@null"
                android:padding="0dp"></TabWidget>

            <FrameLayout
                android:id="@android:id/tabcontent"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:layout_above="@android:id/tabs"></FrameLayout>

        </RelativeLayout>

    </LinearLayout>

</TabHost>